{
    "build": {
      "builder": "nixpacks",
      "config": {
        "nixpacks": {
          "path": "back-end",
          "pythonVersion": "3.11"
        }
      }
    },
    "deploy": {
      "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT"
    }
  }